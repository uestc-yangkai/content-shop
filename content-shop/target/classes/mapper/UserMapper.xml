<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http:// mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.netease.yanxuan.dao.UserMapper">
  <select id="hasUser" resultType="com.netease.yanxuan.meta.User" parameterType="map">
   select userName,userType from user where userName=#{userName} and passWord=#{passWord}
  </select>
</mapper>